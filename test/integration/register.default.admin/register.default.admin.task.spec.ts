describe('REGISTER DEFAULT ADMIN TASK', () => {
    describe('CREATE USER ADMIN', () => {
        context('when create a default Admin successfully', () => {
            it('should return an array of size equal to 1', async () => {
                //
            })
        })
    })
})
