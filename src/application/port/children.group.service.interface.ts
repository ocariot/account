import { IService } from './service.interface'
import { ChildrenGroup } from '../domain/model/children.group'

/**
 * Children Group service interface.
 *
 * @extends {IService<ChildrenGroup>}
 */
export interface IChildrenGroupService extends IService<ChildrenGroup> {
}
